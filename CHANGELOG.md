# 更新日志

所有版本的更新记录都将在此文件中展示。

## [8.2.0] - 2026-08-17

### ✨ 新增功能

#### 1. 钉钉文档支持
- 新增 `dingtalk.com` 和 `alidocs.com` 站点识别
- 适配钉钉 Cangjie 编辑器的正文节点与透明选择层
- 支持在钉钉文档中选中文本并使用 `Ctrl/Cmd+C` 复制
- 防止钉钉页面自身的复制监听器覆盖剪贴板内容

#### 2. 页面主执行环境桥接
- 新增 `main-world.js`，在页面 MAIN world 中处理复制、粘贴和网络响应
- fetch/XHR 复制权限处理现在可作用于网页自身请求
- 关闭总开关或复制功能后自动恢复页面当时的 fetch/XHR 实现

### 🔧 架构优化

#### 1. 设置与状态同步
- 后台脚本成为设置的统一写入入口
- 弹窗、快捷键、右键菜单和内容脚本共享同一份存储状态
- 修复从弹窗切换总开关后图标徽章未及时更新的问题
- 修复右键菜单切换开关后已打开标签页状态不同步的问题

#### 2. 功能生命周期重构
- 复制、粘贴和模拟输入子开关现在分别控制对应功能
- 设置变化时先清理旧监听器和观察器，再按新配置启用
- 避免反复启停造成事件监听器和 MutationObserver 重复叠加
- 浮动输入框重复打开时保持单实例

#### 3. 页面恢复与 DOM 安全
- 记录扩展修改的内联样式和属性，并在关闭时恢复原值
- 站点遮罩改为临时隐藏，不再直接删除页面节点
- 通用解锁只处理实际存在文本选择限制的元素
- 不再全局移除 `disabled`、`readonly` 或强制设置 `pointer-events`
- 不再按模糊的 `overlay`、`mask`、`blur` 类名隐藏普通页面组件

#### 4. iframe 与 Shadow DOM
- 使用 Manifest V3 `all_frames` 分别处理各个 iframe
- 移除父页面对 iframe 的重复递归操作和无效跨域消息尝试
- 保留 Shadow DOM 动态节点检测，并纳入统一清理流程

### 🐛 修复问题
- 修复架构重构后钉钉 Cangjie 编辑器选区被清空时无法复制的回归
- 新增钉钉专用「强制复制当前选区」右键菜单，不再依赖浏览器的 selection 上下文识别
- 修复粘贴子开关仅改变界面、不控制实际行为的问题
- 修复部分站点处理器忽略复制子开关的问题
- 修复内容脚本在隔离环境中重写网页原型但实际不生效的问题
- 修复关闭插件后页面样式、属性和交互状态无法完整恢复的问题
- 修复普通弹窗、遮罩和禁用控件可能被通用规则误伤的问题
- 修复右键复制和粘贴未按触发 frame 执行的问题

### ✅ 验证
- 通过 JavaScript 语法与 Manifest JSON 检查
- 使用真实加载扩展的 Chromium/Edge 环境验证设置同步、徽章、复制和粘贴子开关
- 验证动态 DOM、iframe、主世界 fetch 恢复及钉钉 Cangjie 编辑器复制

---

## [8.1.0] - 2026-02-12

### ✨ 新增功能

#### 1. 浮动输入框增强
- **速度调节**：新增慢速/正常/快速三档输入速度选择
- **暂停/继续**：输入过程中可随时暂停和继续
- **停止按钮**：可随时中断输入

#### 2. Toast 视觉反馈
- 复制成功时显示提示
- 输入完成时显示提示
- 插件启用/禁用时显示提示
- 支持四种提示类型：success、error、info、warning

#### 3. 右键菜单支持
- 选中文本时：「🔓 强制复制选中内容」
- 右键输入框时：「🔓 强制粘贴」
- 任意位置：「🔌 切换插件开关」

#### 4. 新增网站支持
- **百度文库** (wenku.baidu.com)
- **微信公众号** (mp.weixin.qq.com)
- **知网** (cnki.net)
- **知乎专栏** (zhihu.com) - 完善支持

### 🔧 功能优化

#### 快捷键修正
- 修正快捷键提示与实际功能不一致的问题
- `Ctrl+M`：切换插件开关
- `Ctrl+Shift+M`：打开浮动输入框

#### 代码优化
- 移除未实现的站点独立设置功能
- 清理冗余代码


---

## [8.0.0] - 2026-02-11

### ✨ 新增功能

#### 1. 状态指示器
- 插件图标徽章显示当前状态
  - 开启时显示绿色 "ON" 徽章
  - 关闭时无徽章
  - 检测到复制限制时显示橙色 "!" 提示


#### 2. 智能识别复制限制
- 自动检测页面是否有复制限制
- 检测类型包括：
  - CSS 限制 (`user-select: none`)
  - 事件拦截 (`oncopy`, `onpaste` 等)
  - 登录/VIP 遮罩
  - 特定网站类型
- 插件关闭时，检测到限制会在图标上显示提示


#### 3. Shadow DOM 深度处理
- 递归遍历所有 Shadow DOM
- 自动处理动态创建的 Shadow DOM
- 监听 Shadow DOM 内部变化
- 使用 WeakSet 避免重复处理

#### 4. iframe 全面支持
- 处理同源 iframe（直接访问内容）
- 尝试处理跨域 iframe（通过 postMessage）
- 监听新创建的 iframe
- 递归处理嵌套 iframe

#### 5. 智能 DOM 观察器
- 批量处理 DOM 变更（防抖优化）
- 过滤无效变更（文本节点、脚本标签等）
- 标记已处理元素避免重复操作
- 性能统计功能
- 属性变更监听（style、class、disabled、readonly）

### 🌐 新增网站支持（实验性）

- **头歌实践教学平台** (educoder.net)

### 🔧 功能优化

#### 插件架构
- 从油猴脚本迁移到浏览器插件
- 使用 Manifest V3 标准
- 分离 background.js 和 content.js
- 添加完善的错误处理机制

#### 内存管理
- 自动清理 MutationObserver
- 清理事件监听器
- 功能清理回调机制
- 页面卸载时自动清理

#### 性能优化
- 节流和防抖处理
- 批量 DOM 操作
- 避免重复处理相同元素
- 智能过滤无效变更

### 🐛 修复问题

- 修复 CSDN 登录遮罩残留问题
- 修复插件关闭后页面无法交互的问题
- 修复部分网站复制限制无法解除的问题
- 修复浮动输入框在某些网站无法呼出的问题

---

## [7.3.4] - 2026-01-31

### 修复
- 修复了 `utils.createFloatingInputBox` 在定义前被调用的问题
- 添加了 jQuery 存在性检查，防止超星页面在没有 jQuery 时报错
- 移除了有问题的 `removeEventListeners` 函数（该函数未被使用且实现有问题）

### 增强
- 键盘事件监听改为使用捕获阶段，并添加 `stopPropagation()` 阻止事件传播，确保脚本优先处理
- 新增 Shadow DOM 支持，`unlockCssRestrictions` 现在会递归处理 Shadow DOM 内的元素
- 新增 `processAllDocuments` 函数，支持递归处理 iframe 内的文档（同域）
- 新增备用激活方式：**双击输入框**可呼出浮动输入框，作为 `Ctrl+M` 的备用方案

## [7.3.3] - 早期版本

### 功能
- 解除网页复制粘贴限制
- 支持学习通、PTA、CSDN、飞书云文档等网站
- 支持模拟人工输入（Ctrl+M 呼出浮动输入框）
- 自动移除 VIP 遮罩层
- 拦截 XHR 请求修改响应数据
- 针对飞书、PTA、超星等网站提供专用解锁逻辑

### 技术特性
- 使用 `@run-at document-start` 确保脚本尽早执行
- 监听 DOM 变化动态解除限制
- 覆盖原生事件监听方法阻止限制事件

## 版本号说明

本项目使用 [语义化版本](https://semver.org/lang/zh-CN/)：

- **主版本号**：重大功能更新或架构变更
- **次版本号**：新增功能或重大改进
- **修订号**：问题修复或小优化

---

---

## 反馈与建议

如果您有任何建议或发现了 bug，欢迎通过以下方式反馈：

- 邮箱：EchoRunning@outlook.com
- GitHub Issues

感谢您对「解除复制粘贴限制」插件的支持！
